# mcp (not built)

Reserved for an MCP server exposing `inspect`, `unlock` and `list_families`, so
an assistant can unlock a statement itself instead of asking you to.

This is the surface most likely to earn its place next: it turns "unlock this,
then read it" into one step. It would import `pdf_unlock_engine` directly.
